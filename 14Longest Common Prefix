class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
    	string prefix="";
        if sizeof(strs)==0
        	return prefix;

        for (int i =0;i< strs[1].size; i++){
        	for(int j=i; j< sizeof(strs); j++){
        		if strs[j][i]==strs[1][i]
        			continue;
        		else
        			
        	}
        }

        
    }
};
